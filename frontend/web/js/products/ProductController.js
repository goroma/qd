define(function (require) {
    var app = require('../app');

    app.controller('ProductController', ['$scope', '$location', '$window', '$translate',
            function ($scope, $location, $window, $translate) {
            }
    ]);
});

define(function (require) {
    var app = require('../app');

    app.controller('SingleController', ['$scope', '$location', '$window', '$translate',
            function ($scope, $location, $window, $translate) {
            }
    ]);
});

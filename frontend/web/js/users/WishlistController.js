define(function (require) {
    var app = require('../app');

    app.controller('WishlistController', ['$scope', '$location', '$window', '$translate',
            function ($scope, $location, $window, $translate) {
            }
    ]);
});
